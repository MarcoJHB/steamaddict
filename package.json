{
  "name": "steamdb-playtime",
  "version": "1.0.0",
  "description": "Steam playtime tracker dashboard",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build step needed'",
    "fetch": "node scripts/fetch.js"
  },
  "dependencies": {
  "node-fetch": "^3.3.2",
  "@netlify/functions": "^2.8.1"
},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
